let cantidadSegundos = 300;
while (cantidadSegundos > 0) {
  document.write(cantidadSegundos + "<br>");
  cantidadSegundos--;
  if (!cantidadSegundos) {
    document.write("Hemos llegado al final, excelente!");
  }
}
